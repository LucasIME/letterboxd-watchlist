[project]
name = "letterboxd-watchlist"
version = "0.1.0"
description = "Browse a Letterboxd watchlist filtered by streaming availability (Mubi)."
requires-python = ">=3.10"
dependencies = [
    "Flask==3.0.3",
    "requests==2.32.3",
    "beautifulsoup4==4.12.3",
    "python-dotenv==1.0.1",
]

[tool.uv]
package = false
